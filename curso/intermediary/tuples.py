# NOTE Tuplas "()" ou separados por vírgula
# !listas que não podem ser alteradas, nem os valores dos elementos
# ! ou seja, myTuple[0] = 'oi' não pode, nem remover nem adicionar

myTuple = ("Victoria", 20) # Tupla (nome, idade)
myOtherTuple = 'Victoria', 'Ricardo', 'Maria' # Posso criar assim

# NOTE TUDO o que pode se usar com as listas, pode com as tuplas
# Iteração, fatiação, concatenação e etc são válidos em tuplas também
# Tupla para lista -> list(tuple) e o inverso é tuple(list)